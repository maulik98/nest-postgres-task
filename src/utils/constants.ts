export enum ROLES {
  ADMIN = 'admin',
  EDITOR = 'editor',
  VIEWER = 'viewer',
}
export enum INGESTION_STATUS {
  PENDING = 'pending',
  IN_PROGRESS = 'In progress',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
